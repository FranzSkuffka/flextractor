// Generated by CoffeeScript 1.10.0
(function() {
  var Dataset;

  Dataset = (function() {
    function Dataset(type, entities) {
      var dataEntry, entity, i, len;
      this.type = type;
      this.data = [];
      for (i = 0, len = entities.length; i < len; i++) {
        entity = entities[i];
        dataEntry = {};
        dataEntry.name = entity.target.field;
        dataEntry.value = entity.value;
        this.data.push(dataEntry);
      }
    }

    Dataset.prototype.addRelation = function(relationId) {
      return this.relation = relationId;
    };

    return Dataset;

  })();

  module.exports = Dataset;

}).call(this);
